name: sample-2
source:
  type: kafka
  config:
    broker: "localhost:9092"
sinks:
  - name: http
    config:
      method: POST
      url: "http://localhost:9090/metadata"
processors:
  - name: sample-processors
    config:
      foo: bar
