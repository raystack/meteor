name: test-recipe
version: v1beta1
source:
  name: test-source
  config:
    username: {{.source_username}}
    password: "{{.source_password}}"
sinks:
  - name: test-sink
processors:
  - name: test-processor

