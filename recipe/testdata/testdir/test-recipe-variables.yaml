name: test-recipe
version: v1beta1
source:
  name: test-source
  scope: my-scope
  config:
    username: {{.source_username}}
    password: "{{.source_password}}"
sinks:
  - name: test-sink
processors:
  - name: test-processor

